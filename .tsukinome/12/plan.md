# Plan — Issue #12: Add formatDuration(ms) utility for human-readable duration strings

> Generated by Tsukinome. The technical blueprint for the work, approved before any code.

## Summary

Introduce a self-contained TypeScript utility module exporting `formatDuration(ms: number): string`. The function guards against non-finite and non-positive input (returning "0ms"), then rounds to the nearest whole unit at the appropriate scale, applying carry-across logic so that rounding up re-triggers unit selection. It is accompanied by a dedicated unit test file and is wired into nothing.

## Approach

Since the code index is unavailable, plan conservatively using standard TypeScript conventions likely present in a `src/utils` directory with a co-located or `__tests__`/`.test.ts` test file. Implement a single pure function with no dependencies. Key design decision: rather than rounding at a fixed unit and risking boundary carry bugs, compute the rounded value at the target unit and re-check against the next threshold. A clean approach is: (1) validate input — if `!Number.isFinite(ms) || ms <= 0` return "0ms" (note `-0 <= 0` is true, so it is handled naturally); (2) if `ms < 1000` return `${Math.round(ms)}ms`; (3) else round to whole seconds `s = Math.round(ms/1000)`; if `s < 60` return `${s}s`; (4) else round to whole minutes from total seconds but keep seconds — carry: recompute minutes and remaining seconds from `s`, i.e. `m = Math.floor(s/60)`, `rem = s % 60`; if resulting total minutes `< 60` return `${m}m ${rem}s`; (5) for the hour range round to whole minutes `mTotal = Math.round(ms/60000)`, then `h = Math.floor(mTotal/60)`, `remM = mTotal % 60`, return `${h}h ${remM}m`. Choosing to round at each unit's own granularity (ms→ms, seconds range→seconds, minutes range→minutes) ensures carry examples (59999→1m 0s via s=60; 3599999→1h 0m via mTotal=60) resolve correctly. Guard the hour-selection threshold using the rounded-second/minute values so a carry that crosses 3600000 uses hour formatting.

## Affected Files

- `src/utils/formatDuration.ts` **(add)** — New dedicated module exporting the pure `formatDuration(ms: number): string` utility.
- `src/utils/formatDuration.test.ts` **(add)** — Unit tests covering all acceptance criteria including issue examples and edge cases.

## Contracts

- export function formatDuration(ms: number): string — returns a compact human-readable duration string; returns "0ms" for non-finite or ms <= 0; "{ms}ms" for 0<ms<1000; "{s}s" for the second range; "{m}m {s}s" for the minute range; "{h}h {m}m" for the hour range, with nearest-unit rounding and carry across boundaries.

## Data Changes

_None._

## Test Strategy

- Write the test file first against the not-yet-created module (red), then implement `formatDuration` to make all cases green.
- Guard/edge tests: AC1 (0→"0ms"), AC13 (-100→"0ms"), AC14 (NaN→"0ms"), AC15 (Infinity→"0ms"), AC16 (-Infinity→"0ms"), AC17 (-0→"0ms").
- Millisecond range: AC2 (820→"820ms"), AC3 (820.6→"821ms" verifying nearest-integer rounding), AC4 (999→"999ms").
- Second range: AC5 (1000→"1s" boundary), AC6 (5000→"5s").
- Minute range and carry: AC7 (59999→"1m 0s" carry from seconds), AC8 (60000→"1m 0s" boundary), AC9 (65000→"1m 5s"), AC10 (65999→"1m 6s" second rounding).
- Hour range and carry: AC11 (3599999→"1h 0m" carry to hour), AC12 (3600000→"1h 0m" boundary).
- Add a very-large-value sanity assertion (e.g. tens of hours) to confirm hours/minutes formatting per edge cases, and confirm the module exports nothing beyond `formatDuration` and adds no imports/dependencies (R10).
